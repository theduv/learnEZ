import { memo } from "react";
import { Typography } from "../../ui/Typography";

const LearnBase = () => {
  return (
    <div>
      <Typography variant="h1">Work in progress...</Typography>
    </div>
  );
};

export const Learn = memo(LearnBase);
